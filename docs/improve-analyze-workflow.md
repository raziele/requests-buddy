- When summarizing emails and attachements, the output file name should represent the NGO / request
- Move prompts from code (normalize_requests.py) to md files, then have the code echo them into the agent
- Use Cursor agent using `curl https://cursor.com/install -fsS | bash)`
- Use Curser agent with api key: `agent --api-key $CURSOR_API_KEY "hi there"`
- Use print mode to print the response to the console `agent -p "hi"`
- run in yolo mode to auto-approve actions
- refer files inside the prompt: `agent --api-key $CURSOR_API_KEY "summarize ./raw_emails/20260309-133411/Mon, 9 Mar-fw-zaka-operational-needs/Jewish Federation of Miami  -ZAKA National Emergency Rescue Equipment Initiative 080326.pdf" -p`
-- Note: path to files MUST be relative to the project directory
- always run in auto mode `--model auto`
- run with `--output-format text`
- run with `--trust`

