Merge the following duplicate request documents into a single unified Markdown document.

Output ONLY the merged Markdown document — no explanation, no commentary, no preamble. The first character of your response must be the `---` that opens the YAML frontmatter.

Rules:
- Keep the YAML frontmatter (between `---` delimiters). Preserve all fields from the source documents. Add `merged_from` as a list of the original file paths.
- Preserve ALL unique information from every source document.
- Keep the body well-organized — combine content, remove redundancy, note any differences.

{{documents}}
